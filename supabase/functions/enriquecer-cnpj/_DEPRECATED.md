# DEPRECATED — enriquecer-cnpj

This Edge Function is **dead code** and is not called from any part of the application (ERP or App de Campo).

It was originally intended to enrich CNPJ data from an external API, but the feature was never wired into the UI.

**Safe to remove** in a future cleanup sprint.

Last reviewed: 2026-03-22
