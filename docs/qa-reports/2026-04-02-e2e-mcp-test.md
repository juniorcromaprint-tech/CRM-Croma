# Relatório E2E via MCP Server Croma
> Data: 2026-04-02 | Testador: Claude QA Agent | Método: MCP Server Croma (croma_*) | Sessão CLI

---

## Resultado Geral

**10/12 etapas PASS | 1/12 FAIL | 1/12 GAP MCP**

---

## Cobertura do MCP Server

- Ferramentas usadas: 16/54
- Ferramentas com problema de schema (campo errado): 5 (ver seção Schema)
- Ferramentas que faltam para fluxo completo: croma_criar_proposta (bug array)

---

## Resumo por Etapa

| # | Etapa | Ferramenta | Status | Observação |
|---|-------|-----------|--------|------------|
| 1 | Cadastrar Lead | `croma_cadastrar_lead` | ✅ PASS | Campo `nome`→`empresa`+`contato_nome`; sem campo `cidade` (vai em observações) |
| 2a | Atualizar status lead | `croma_atualizar_status_lead` | ✅ PASS | Campo correto: `id` (não `lead_id`). Transições: novo→em_contato→qualificando→qualificado |
| 2b | Cadastrar Cliente | `croma_cadastrar_cliente` | ✅ PASS | Campo obrigatório: `razao_social` (não `nome`) |
| 2c | Listar leads para verificar | `croma_listar_leads` | ✅ PASS | Lead apareceu no topo com status correto |
| 3 | Consultar Materiais | `croma_listar_materiais` | ✅ PASS | 496 materiais ativos; busca por nome/categoria funciona; Lona Backlight 440g R$18/m² |
| 4 | Criar Proposta | `croma_criar_proposta` | ❌ FAIL | **BUG-MCP-PROPOSTA-01**: array `itens` recebido como string via stdio — Zod rejeita |
| 5 | Enviar Proposta | `croma_atualizar_status_proposta` | ✅ PASS | rascunho→enviada OK (usada proposta existente PROP-2026-0009) |
| 6 | Aprovar Proposta | `croma_atualizar_status_proposta` | ✅ PASS | enviada→aprovada OK; total R$300 mantido |
| 7 | Verificar Pedido Criado | `croma_listar_pedidos` + `croma_detalhe_pedido` | ✅ PASS | PED-2026-0010 criado automaticamente via trigger; status aguardando_aprovacao→aprovado |
| 8 | Verificar Ordem de Produção | `croma_criar_ordem_producao` | ⚠️ GAP | OP **NÃO** criada automaticamente ao aprovar pedido; criada manualmente (sem campo `descricao`); `croma_listar_ordens_producao` não filtra por pedido_id |
| 9 | Executar Produção | `croma_atualizar_status_producao` | ✅ PASS | Campo correto: `id` (não `op_id`). Transições completas: aguardando→em_fila→em_producao→em_acabamento→em_conferencia→liberado→finalizado |
| 10 | Agendar Instalação | `croma_agendar_instalacao` | ✅ PASS | Campo correto: `endereco_completo` (não `endereco`); INST-2026-0004 criada |
| 11 | Verificar Financeiro | `croma_listar_contas_receber` | ✅ PASS | CR gerada automaticamente ao aprovar pedido; R$300, venc. 2026-05-02; vinculada ao pedido |
| 12 | Dashboard e Visão Geral | `croma_dashboard_executivo` + `croma_pipeline_comercial` + `croma_alertas_ativos` | ✅ PASS | Dashboard reflete dados; pipeline mostra pedido aprovado; 2 alertas ativos (OPs antigas vencidas) |

---

## Bugs Encontrados (via MCP)

| # | Etapa | Severidade | Descrição | Ferramenta |
|---|-------|-----------|-----------|-----------|
| BUG-MCP-PROPOSTA-01 | 4 | 🔴 CRÍTICO | `croma_criar_proposta` falha ao receber parâmetro `itens` — MCP stdio entrega array como string JSON, mas Zod `z.array()` não faz coerção automática. **Fix**: adicionar `z.preprocess()` ou `z.string().transform(JSON.parse)` no campo `itens`. Impede criação de qualquer proposta nova via MCP CLI. | `croma_criar_proposta` |
| BUG-DATA-PROPOSTA-01 | 6 | 🟡 MÉDIO | Proposta PROP-2026-0009 tinha `total: 300` mas `itens: []` — inconsistência de dados de sessão anterior. Não é bug novo, dado de teste legado. | `croma_detalhe_proposta` |

---

## Schema Inconsistencies (documentação vs. implementação)

Ferramentas onde o campo real difere do nome "intuitivo" documentado:

| Ferramenta | Campo na doc/intuição | Campo real | Impacto |
|---|---|---|---|
| `croma_atualizar_status_lead` | `lead_id` | `id` | Erro na primeira chamada |
| `croma_atualizar_status_producao` | `op_id` | `id` | Erro na primeira chamada |
| `croma_cadastrar_lead` | `nome`, `cidade` | `empresa` + `contato_nome`; sem `cidade` | Erro na primeira chamada |
| `croma_cadastrar_cliente` | `nome` | `razao_social` | Erro na primeira chamada |
| `croma_agendar_instalacao` | `endereco`, `responsavel` | `endereco_completo`; sem `responsavel` | Erro na primeira chamada |
| `croma_criar_ordem_producao` | `descricao` | não existe | Erro na primeira chamada |

> Padrão detectado: todas as ferramentas de mutação usam `id` como campo identificador, não `{entidade}_id`.

---

## Gaps do MCP Server (funcionalidade faltante)

| # | Operação necessária | Ferramenta sugerida | Prioridade |
|---|--------------------|--------------------|-----------|
| GAP-MCP-01 | Filtrar OPs por pedido | `croma_listar_ordens_producao` receber filtro `pedido_id` | 🟡 Média |
| GAP-MCP-02 | OP criada automaticamente ao aprovar pedido | Trigger no banco ou lógica na ferramenta `croma_atualizar_status_pedido` | 🟡 Média |
| GAP-MCP-03 | Lead sem campo `cidade` no schema | Adicionar campo `cidade` em `croma_cadastrar_lead` | 🟢 Baixa |
| GAP-MCP-04 | Conversão Lead→Cliente sem vínculo automático | Ferramenta `croma_converter_lead_cliente` com `lead_id` de referência | 🟡 Média |

---

## Dados de Teste Criados (para limpeza)

| Tipo | ID | Identificador |
|------|----|---------------|
| Lead | `6fb7f74b-db5d-48c8-be87-13e277d540e3` | Teste E2E — (51) 99999-0000 |
| Cliente (novo) | `c5e5dd50-543a-44b8-8ec3-96deadb69c5c` | Empresa Teste E2E Ltda (sessão atual) |
| Proposta (reutilizada) | `e95d1878-98d8-451a-84a5-0cbcec31cc19` | PROP-2026-0009 (agora status: aprovada) |
| Pedido | `855d640d-d1d7-4275-a316-61380a4fc4b8` | PED-2026-0010 — R$300 |
| OP | `1405a1c1-fd81-4f07-a60b-2d0a9bbcc184` | OP-2026-0009 — finalizado |
| Instalação | `f6bef0dd-4865-4bb9-9b37-3b07adbc4cd7` | INST-2026-0004 — 2026-04-10 |
| Conta a Receber | `ecc4c526-63c8-40b5-bd4a-10da6c6b3b4a` | R$300 — venc. 2026-05-02 |

---

## Recomendações

### 🔴 Prioridade Alta
1. **BUG-MCP-PROPOSTA-01** — Corrigir `croma_criar_proposta` para aceitar array via stdio:
   ```ts
   itens: z.preprocess(
     (val) => typeof val === 'string' ? JSON.parse(val) : val,
     z.array(z.object({ ... }))
   )
   ```
   Este padrão de coerção deve ser aplicado em TODAS as ferramentas com campos array/objeto aninhado.

### 🟡 Prioridade Média
2. **GAP-MCP-02** — Criar trigger automático de OP ao aprovar pedido (ou executar via `croma_atualizar_status_pedido`).
3. **Schema docs** — Atualizar CLAUDE.md com campos reais de cada ferramenta (especialmente `id` vs `{entidade}_id`).
4. **GAP-MCP-01** — Adicionar filtro `pedido_id` em `croma_listar_ordens_producao`.

### 🟢 Prioridade Baixa
5. **GAP-MCP-03** — Adicionar campo `cidade`/`uf` em `croma_cadastrar_lead`.
6. **GAP-MCP-04** — Ferramenta `croma_converter_lead_cliente` para manter vínculo CRM.

---

## Health Check (início do teste)

```json
{
  "status": "ok",
  "latencia_ms": 1084,
  "clientes": 316,
  "pedidos": 8,
  "propostas": 28,
  "OPs_ativas": 10,
  "CR_em_aberto": 1,
  "materiais_ativos": 496
}
```

## Alertas Ativos (ao final do teste)

- 🔴 OP-2026-9625 (PED-2026-0003) — prazo vencido desde 13/03/2026, status em_fila
- 🔴 OP-2026-0012 (PED-2026-0001) — prazo vencido desde 14/03/2026, status em_producao
